package com.iCode.week12_day03_backend;

import java.util.ArrayList;

class DuplicatesArray {
	
	private List<String> dup = new ArrayList<>();
	dup.addAll(Arrays.asList("a", "b", "a", "c", "b", "d"));

	
	public void removeDuplicates() {
		for(String g : dup) {
			
		}
	}
}
