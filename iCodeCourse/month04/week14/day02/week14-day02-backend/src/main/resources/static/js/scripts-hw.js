function min(a, b) {
    if (a < b) {
        return a;
    }
    return b;
}
console.log(min(4, 6));

function isAdult(age) {
	if (age >= 18) return "Adult";
	else return "Child";
}
console.log(isAdult(23));

function createCourse(title, week) {
	
}